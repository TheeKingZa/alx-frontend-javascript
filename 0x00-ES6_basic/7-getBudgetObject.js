// Define a function called getBudgetObject using object property value shorthand syntax
export default function getBudgetObject(income, gdp, capita) {
  // Use object property value shorthand syntax to declare the object properties
  const budget = {
    income,
    gdp,
    capita,
  };

  return budget;
}
